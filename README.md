# 🧸 小确幸 | 时光留言墙 (Memory Wall)

一个基于 Vue 3 和 Tailwind CSS 构建的现代极简风格留言墙。支持瀑布流布局、图片懒加载、自动轮播展示，无需后端数据库，仅需维护一个 CSV 文件即可运行。非常适合作为个人回忆录、团队照片墙或数字标牌展示。

## ✨ 功能特性

  * **无需后端**：纯静态 HTML 页面，数据来源于本地 CSV 文件，部署简单（GitHub Pages 完美支持）。
  * **现代美学**：
      * **瀑布流布局**：自适应不同屏幕，从手机单列到大屏四列自动切换。
      * **沉浸式体验**：支持九宫格缩略图，点击图片进入灯箱（Lightbox）查看高清大图。
      * **细节交互**：卡片悬停浮动、优雅的过渡动画、毛玻璃（Backdrop blur）效果。
  * **数字标牌模式**：内置「自动轮播」功能，开启后每 60 秒自动洗牌刷新，适合投放在大屏幕上作为展示墙。
  * **高性能**：
      * 图片原生懒加载 (`loading="lazy"`).
      * 无限滚动加载（Infinite Scroll），避免一次性渲染大量数据卡顿。
  * **移动端优先**：针对手机端专门优化的双层顶部布局，操作更便捷。

## 📂 目录结构

您的项目文件夹应包含以下核心文件：

```text
.
├── index.html          # 主页面（直接浏览器打开即可，但在本地需开启 HTTP 服务）
├── datacvs.csv         # 数据源文件
├── images/             # 图片文件夹，存放所有留言图片
│   ├── 20251203001.png
│   ├── 20251203001(1).png
│   └── ...
└── README.md           # 说明文档
```

## 🚀 快速开始

### 1\. 本地预览

由于浏览器安全策略（CORS），直接双击打开 `index.html` 可能会提示“无法加载 CSV”。请使用以下任意一种方式启动本地服务器：

  * **VS Code (推荐)**: 安装 "Live Server" 插件，右键 `index.html` 选择 `Open with Live Server`。
  * **Python**: 在项目目录下打开终端，运行 `python -m http.server`，然后访问 `http://localhost:8000`。

### 2\. 部署到 GitHub Pages

1.  将项目所有文件上传到 GitHub 仓库。
2.  进入仓库 **Settings** -\> **Pages**。
3.  在 **Build and deployment** 下，选择 **Source** 为 `Deploy from a branch`，分支选择 `main` / `root`。
4.  保存后等待几分钟，即可通过 `https://你的用户名.github.io/仓库名/` 访问。

## 📝 数据配置说明

### 1\. 编辑 CSV (`datacvs.csv`)

请使用 Excel 或文本编辑器维护 `datacvs.csv`，格式如下：

| id | 标题 | 日期 | 留言 | 图片 | 状态 | 是否有图 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 20251203001 | 温暖 | 2025/12/03 09:31 | 这里写内容... | 2 (图片数量) | 可展示 | 1 |

  * **id**: 唯一标识，**必须与文件名对应**。
  * **图片**: 填写图片的**数量**（例如有3张图，就填 `3`；如果没有图留空或填其他内容）。
  * **是否有图**: `1` 表示有图，`0` 表示无图（用于筛选）。
  * **状态**: 必须填 `可展示` 才会显示在页面上。

### 2\. 图片命名规则

将图片放入 `images/` 文件夹，必须严格遵守以下命名规则（支持 `.png` 或 `.jpg`）：

  * **第 1 张图**：`{id}.png`
  * **第 2 张图**：`{id}(1).png`
  * **第 3 张图**：`{id}(2).png`
  * ...以此类推

> **示例**：
> 如果 CSV 中 ID 为 `20251203001`，且有 3 张图：
>
> 1.  `images/20251203001.png`
> 2.  `images/20251203001(1).png`
> 3.  `images/20251203001(2).png`

## 🛠️ 实用工具：图片批量压缩

为了加快网页加载速度，建议在上载前压缩图片。您可以使用以下 Python 脚本自动压缩 `images` 文件夹下大于 300KB 的图片：

```python
# 保存为 compress.py 并运行
import os
from PIL import Image

def compress_images(folder_path, max_size_kb=300):
    for filename in os.listdir(folder_path):
        if filename.lower().endswith(('.png', '.jpg', '.jpeg')):
            file_path = os.path.join(folder_path, filename)
            if os.path.getsize(file_path) / 1024 > max_size_kb:
                try:
                    with Image.open(file_path) as img:
                        # PNG 转 JPG 压缩或重新保存
                        img.convert('RGB').save(file_path, "JPEG", optimize=True, quality=70)
                        print(f"已压缩: {filename}")
                except Exception as e:
                    print(f"压缩失败: {filename}")

if __name__ == "__main__":
    compress_images("./images")
```

## 📄 License

MIT License. Feel free to use and modify.
